import * as types from '../types';

const initialState = {};

const toDoReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default toDoReducer;